{
  "name": "koalaclicker",
  "version": "1.2.9",
  "description": "KoalaClicker release build tooling",
  "private": true,
  "scripts": {
    "build:extension": "node scripts/build-extension.cjs",
    "test": "node --test scripts/test-extension.mjs",
    "check": "npm run build:extension && npm test"
  },
  "devDependencies": {
    "archiver": "^7.0.1"
  },
  "overrides": {
    "glob": "13.0.6"
  }
}
